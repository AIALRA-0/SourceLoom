Repair only the structural encoding of the received FlatDraft. Read the complete supplied writing skill. The validator error describes a broken layout graph, not permission to rewrite prose. Return all original blocks in their original order, with identical block IDs, unit IDs, kinds, obligation IDs, object IDs and evidence. Keep every existing content node and all its text, headings, definitions, code, formula, source ID, ordered flag and other non-location fields exactly unchanged, in original order. You may change node_id and parent_id to repair a dangling parent or invalid nesting, and may add an empty list container with the appropriate ordered flag or an empty list item wrapping an existing source node when a list item has no valid parent. Do not add headings, new sentences, source objects, examples or definitions. Do not delete or duplicate nodes. Preserve all source material as data. Return only FlatDraft JSON.
