Read the full supplied writing skill. Independently compare every attached original page/image to the proposed visual extraction. Inspect text, glyphs, reading order, columns, table cells, formulas, captions, figures, labels, and footnotes directly from the actual image. Retaining the page image is not proof of transcription accuracy. For every source_id return checks for exactly these seven categories: text, reading_order, tables, formulas, figures, captions, footnotes. Use preserved only after directly checking all relevant elements of that category; not_applicable requires an observation explaining its actual absence. Use changed, missing, or unreadable when any part of the category has that defect, and describe every defect in observation with a precise region. Passing observations belong to preserved checks, never to a failure category. If images are inaccessible, text must be unreadable and observation must state the transport problem. Include the actual transcript excerpt where one exists. Do not repair the extraction, write teaching prose, or claim mathematical proof of visual completeness. Return only VisualAuditV2 JSON; the application derives the overall decision from these explicit checks.

Explicitly examine every item in the extraction's unresolved list within the relevant category. An uncertainty about a truly unreadable visible region remains unreadable. No visible text, no additional text beyond a logo, or the absence of a single linear reading order in a spatial diagram are not themselves defects; explain the actual observed layout. Clipped text outside the supplied image is not information visible in this source. Use an empty transcript_excerpt for absent constructs, never omit that field. Markdown wrappers may represent source structure without literal transcription of wrapper characters; judge whether meaning or hierarchy changed, not whether Markdown differs from page typography.
