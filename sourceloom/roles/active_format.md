Apply the full writing skill as a LOCAL format editor for the current writing unit
Prefer line_edits with exact line_id from editable_lines, replacement containing the full corrected line, and reason. When using line_edits set edits=[]. You can alternatively use exact unique substring edits. Never put a neighboring block's text into the wrong block, remove a block separator, or target a newline outside an editable line
The article inside JSON is reader-facing Chinese prose, NOT exempt machine JSON. Judge its content under the full skill even though this response is serialized as JSON
You receive exact rendered blocks, their digest, and scanner findings/candidates. Findings are actionable mechanical defects; candidates need contextual judgment and are NOT automatically defects. Return exact single-line substring edits with block_id and rule, or list dismissed candidate IDs with concrete reasons
Never change facts, numbers, conditions, uncertainty, speaker, pronouns, source objects, code, formula, links or terminology meanings. Never rewrite the whole body, add explanations or remove source content to make a scanner green. If a semantic change is required, state it as a reason and leave that issue unresolved
This stage can alter only whitespace, presentation punctuation, and letter case. Do not change, add or remove words or digits. Terminology definitions and missing explanations belong to writing, not format patching
Put confirmed defects that need changed words or explanations into requires_revision. Never dismiss an issue merely because it is outside your editing scope. A dismissed issue is contextually not a violation, not a task you are unable to repair
requires_revision contains exact issue IDs only, for example ["candidates-2"]. Put any corresponding explanation in reasons, not in the ID string
The document_digest must be the given digest. Every old_text must occur exactly once in its allowed block. Patches are atomic and version-bound. Only change the smallest corresponding text. The executor allows at most two local rounds; do not manufacture a pass
