You plan a faithful rewrite of the supplied source, in an independent call. Read the complete writing skill. Return only the requested Plan JSON, not prose, a course, or an audit verdict. Organize the original subject and its real dependencies; keep every source fact, heading relationship, protected object, narrator, pronoun, condition, exception and link. Use only supplied fact and object IDs. Metadata belongs in document_info_ids and end matter unless it is itself the source subject.

The full writing skill is mandatory in this user task. The plan MUST NOT waive first-use definitions, bilingual terminology, verified acronyms, legal commented copies of code, or explanations needed to understand the actual source. Minimal necessary explanation means explaining the source accurately without unrelated detours; it does NOT mean copying a terse definition and forbidding its missing prerequisites. Use supplied verified_terminology and its evidence where applicable. Never explicitly prohibit an acronym expansion when the current task requires it and verified evidence is provided. Do not copy malformed term labels into suggested prose. Source English headings may be translated while their identity and hierarchy remain linked.

Use ordinary language to introduce the source's real topic; do not introduce technical labels in an overview before their required definition. Plan each first-use definition at the point it is needed, with the complete five-aspect explanation prescribed by the skill. Avoid a duplicate prose definition followed by a second definition item. Follow original code and examples instead of inventing a teaching scenario, numbers, exercises or extensions. Code originals remain byte-identical, followed by separate legal explanation copies. Preserve exact comparisons such as greater-than-or-equal; do not shorten them into equality. Explain directly in the original voice, without narration about preserving source data.

Prefer one coherent processing unit when the source fits. max_units is a hard limit; processing units are not visible headings. Within stages organize a consistent topical heading hierarchy and real transitions. Assign every source heading to an actual translated subject heading, every fact to actual prose, every link to visible linked text. Use prerequisites, reader_question, entry_knowledge, example_thread, learning_result, follows_units and bridge_reason to describe understanding THIS source, not a new curriculum. teaching_functions may be an empty array in rewrite mode. Research gaps contain only essential missing evidence, not unrelated encyclopedic questions. A working length target is soft: avoid duplication and internal bookkeeping prose, never omit information to meet it.

Before returning, compare the proposed stages and prohibitions with the full skill and the explicit rewrite task. No stage or proof question may direct the writer to break those requirements. The plan cannot claim final prose compliance before writing.
